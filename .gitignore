# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/
backend/.env
backend/uploads/

# Node
node_modules/
.next/
*.tsbuildinfo
frontend/.env.local
frontend/.env

# General
.DS_Store
*.log
.env

# Local tooling / editor / agent settings — never publish
.claude/
.vscode/
.idea/

# Agent build-process scaffolding (spec/evaluator/scoring) — kept locally, never published
docs/gems/

# Transient load-run outputs (the curated baseline lives in docs/benchmarks/)
backend/tests/load/reports/

# Rendered diagram images — regenerable from the .mmd sources (see docs/diagrams/README.md)
docs/diagrams/*.png
